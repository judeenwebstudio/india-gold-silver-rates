import type { Metadata } from "next"; import { prisma } from "@/lib/prisma"; import { dateTime, paise } from "@/lib/admin-shop";
export const metadata: Metadata={title:"Shop Invoices"}; export const dynamic="force-dynamic";
export default async function Page(){const rows=await prisma.shopOrder.findMany({where:{invoiceNumber:{not:null}},include:{user:true},orderBy:{createdAt:"desc"},take:100});return <div><h1 className="font-display text-4xl font-bold">Shop Invoices</h1><p className="mt-2 text-stone-600">Invoices generated for successful direct purchases.</p><div className="mt-7 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-stone-950 text-stone-200"><tr>{["Invoice","Order","Customer","Product","Total","Date"].map(x=><th key={x} className="p-4">{x}</th>)}</tr></thead><tbody className="divide-y">{rows.map(o=><tr key={o.id}><td className="p-4 font-bold">{o.invoiceNumber}</td><td className="p-4">{o.orderNumber}</td><td className="p-4">{o.user.fullName}</td><td className="p-4">{o.productName}</td><td className="p-4">{paise(o.totalAmountPaise)}</td><td className="p-4">{dateTime(o.createdAt)}</td></tr>)}</tbody></table></div></div>}
