import type { Metadata } from "next"; import { prisma } from "@/lib/prisma"; import { dateTime, paise } from "@/lib/admin-shop"; import { requireOrderAdmin } from "@/lib/admin-orders";
export const metadata: Metadata={title:"Shop Invoices"}; export const dynamic="force-dynamic";
export default async function Page(){await requireOrderAdmin("view");const rows=await prisma.shopOrder.findMany({where:{invoiceNumber:{not:null},paymentStatus:"SUCCESS"},include:{user:true},orderBy:{createdAt:"desc"},take:100});return <div><h1 className="font-display text-4xl font-bold">Shop Invoices</h1><p className="mt-2 text-stone-600">Premium PDF invoices generated for successful direct purchases.</p><div className="mt-7 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[920px] text-left text-sm"><thead className="bg-stone-950 text-stone-200"><tr>{["Invoice","Order","Customer","Product","Total","Date","Actions"].map(x=><th key={x} className="p-4">{x}</th>)}</tr></thead><tbody className="divide-y">{rows.map(o=><tr key={o.id}><td className="p-4 font-bold"><a className="text-amber-800 underline" target="_blank" rel="noreferrer" href={`/api/v1/admin/orders/${o.id}/document?type=invoice`}>{o.invoiceNumber}</a></td><td className="p-4">{o.orderNumber}</td><td className="p-4">{o.user.fullName}</td><td className="p-4">{o.productName}</td><td className="p-4">{paise(o.totalAmountPaise)}</td><td className="p-4">{dateTime(o.createdAt)}</td><td className="p-4"><div className="flex gap-2"><a href={`/api/v1/admin/orders/${o.id}/document?type=invoice`} target="_blank" rel="noreferrer" className="rounded-lg border px-3 py-2 font-bold">View PDF</a><a href={`/api/v1/admin/orders/${o.id}/document?type=invoice&download=1`} className="rounded-lg bg-stone-950 px-3 py-2 font-bold text-amber-300">Download PDF</a></div></td></tr>)}</tbody></table></div></div>}
