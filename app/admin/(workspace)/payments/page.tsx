import type { Metadata } from "next"; import { prisma } from "@/lib/prisma"; import { dateTime, paise } from "@/lib/admin-shop";
export const metadata: Metadata = { title: "Shop Payments" }; export const dynamic = "force-dynamic";
export default async function Page() { const rows=await prisma.shopOrder.findMany({include:{user:true},orderBy:{createdAt:"desc"},take:100}); return <div><h1 className="font-display text-4xl font-bold">Shop Payments</h1><p className="mt-2 text-stone-600">Razorpay and PhonePe transactions for direct Shop orders. Scheme instalments are excluded.</p><div className="mt-7 overflow-x-auto rounded-2xl border bg-white"><table className="min-w-[1000px] w-full text-left text-sm"><thead className="bg-stone-950 text-stone-200"><tr>{["Order","Customer","Gateway","Gateway order","Gateway payment","Amount","Status","Date"].map(x=><th key={x} className="p-4">{x}</th>)}</tr></thead><tbody className="divide-y">{rows.map(o=><tr key={o.id}><td className="p-4 font-bold">{o.orderNumber}</td><td className="p-4">{o.user.fullName}</td><td className="p-4">{o.gateway}</td><td className="p-4">{o.gatewayOrderId||"—"}</td><td className="p-4">{o.gatewayPaymentId||"—"}</td><td className="p-4">{paise(o.totalAmountPaise)}</td><td className="p-4">{o.paymentStatus}</td><td className="p-4">{dateTime(o.createdAt)}</td></tr>)}</tbody></table></div></div>; }
