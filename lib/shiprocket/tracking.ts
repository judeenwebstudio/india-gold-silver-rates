export type MappedTracking={shipmentStatus:"READY_TO_SHIP"|"PICKUP_SCHEDULED"|"SHIPPED"|"IN_TRANSIT"|"OUT_FOR_DELIVERY"|"DELIVERED"|"CANCELLED"|"RETURNED";orderStatus:"READY_TO_SHIP"|"SHIPPED"|"IN_TRANSIT"|"OUT_FOR_DELIVERY"|"DELIVERED"|"CANCELLED"|"RETURNED"|null;integrationStatus:"AWB_ASSIGNED"|"PICKUP_SCHEDULED"|"IN_TRANSIT"|"DELIVERED"|"CANCELLED"|"RTO"|"FAILED";publicMessage:string;terminal:boolean};
export function mapShiprocketStatus(raw:string):MappedTracking{const value=raw.trim().toUpperCase().replace(/[^A-Z0-9]+/g," ");if(/RTO DELIVERED|RETURNED/.test(value))return {shipmentStatus:"RETURNED",orderStatus:"RETURNED",integrationStatus:"RTO",publicMessage:"The shipment has returned to the sender.",terminal:true};if(/DELIVERED/.test(value)&&!/UNDELIVERED/.test(value))return {shipmentStatus:"DELIVERED",orderStatus:"DELIVERED",integrationStatus:"DELIVERED",publicMessage:"Your order has been delivered.",terminal:true};if(/CANCEL/.test(value))return {shipmentStatus:"CANCELLED",orderStatus:"CANCELLED",integrationStatus:"CANCELLED",publicMessage:"The shipment was cancelled.",terminal:true};if(/RTO|RETURN TO ORIGIN/.test(value))return {shipmentStatus:"IN_TRANSIT",orderStatus:null,integrationStatus:"RTO",publicMessage:"The shipment is returning to the sender.",terminal:false};if(/OUT FOR DELIVERY/.test(value))return {shipmentStatus:"OUT_FOR_DELIVERY",orderStatus:"OUT_FOR_DELIVERY",integrationStatus:"IN_TRANSIT",publicMessage:"Your order is out for delivery.",terminal:false};if(/IN TRANSIT|DESTINATION HUB|PICKED UP|SHIPPED/.test(value))return {shipmentStatus:"IN_TRANSIT",orderStatus:"IN_TRANSIT",integrationStatus:"IN_TRANSIT",publicMessage:/PICKED UP/.test(value)?"The courier has picked up your order.":"Your order is in transit.",terminal:false};if(/PICKUP SCHEDULE/.test(value))return {shipmentStatus:"PICKUP_SCHEDULED",orderStatus:null,integrationStatus:"PICKUP_SCHEDULED",publicMessage:"Courier pickup has been scheduled.",terminal:false};if(/AWB|BOOKED|NEW/.test(value))return {shipmentStatus:"READY_TO_SHIP",orderStatus:"READY_TO_SHIP",integrationStatus:"AWB_ASSIGNED",publicMessage:"Your shipment is booked and awaiting pickup.",terminal:false};if(/LOST|DAMAGED/.test(value))return {shipmentStatus:"IN_TRANSIT",orderStatus:null,integrationStatus:"FAILED",publicMessage:"Your shipment needs courier attention. Our support team is monitoring it.",terminal:false};if(/UNDELIVERED|DELAY|EXCEPTION|NDR/.test(value))return {shipmentStatus:"IN_TRANSIT",orderStatus:null,integrationStatus:"IN_TRANSIT",publicMessage:"Delivery needs attention and is being coordinated with the courier.",terminal:false};return {shipmentStatus:"IN_TRANSIT",orderStatus:null,integrationStatus:"IN_TRANSIT",publicMessage:"Shipment tracking was updated.",terminal:false}}
export const statusRank=(status:string)=>({NOT_CREATED:0,READY_TO_SHIP:1,PICKUP_SCHEDULED:2,SHIPPED:3,IN_TRANSIT:4,OUT_FOR_DELIVERY:5,DELIVERED:10,CANCELLED:10,RETURNED:10}[status]??0);
export function shouldApplyTracking(current:string,next:string){if(["DELIVERED","CANCELLED","RETURNED"].includes(current))return current===next;return statusRank(next)>=statusRank(current)}
