import assert from "node:assert/strict";
import fs from "node:fs/promises";
import test from "node:test";
test("coverage normalizer handles exact, alias, unsupported, failed and CSV fallback data",async()=>{const source=await fs.readFile("lib/goodreturns-coverage.ts","utf8");assert.match(source,/report\.correctMappings/);assert.match(source,/report\.aliasMappings/);assert.match(source,/new Map<string, SupportedRow>/);assert.match(source,/report\.missingMappings/);assert.match(source,/report\.failedCities/);assert.match(source,/csvRows/)});
test("search, filters, sorting and pagination are applied server-side",async()=>{const source=await fs.readFile("lib/goodreturns-coverage.ts","utf8");assert.match(source,/values\.includes\(q\)/);assert.match(source,/row\.mappingType\.toLocaleLowerCase\(\)===mapping/);assert.match(source,/\[25,50,100\]\.includes\(Number\(filters\.size\)\)/);assert.match(source,/rows\.slice\(\(page-1\)\*size,page\*size\)/)});
test("latest query ignores per-city logs and selects only summary records",async()=>{const source=await fs.readFile("lib/goodreturns-coverage.ts","utf8");assert.match(source,/message: \{ startsWith: "GoodReturns city mapping completed" \}/);assert.match(source,/status: \{ in: \["SUCCESS", "NO_CHANGE"\] \}/);assert.doesNotMatch(source,/Synchronized 4 changed rates/)});
test("page is authenticated and never serializes rawData",async()=>{const [page,guard]=await Promise.all([fs.readFile("app/admin/(workspace)/goodreturns-cities/page.tsx","utf8"),fs.readFile("lib/customer-admin.ts","utf8")]);assert.match(page,/requireCustomerAdmin\("REPORTS_VIEW"\)/);assert.match(guard,/ADMIN_UNAUTHORIZED/);assert.doesNotMatch(page,/rawData/)});
test("CSV export respects filters and creates an audit record",async()=>{const route=await fs.readFile("app/admin/(workspace)/goodreturns-cities/export/route.ts","utf8");assert.match(route,/filteredCoverage\(report/);assert.match(route,/GOODRETURNS_COVERAGE_CSV_EXPORTED/);assert.match(route,/REPORTS_EXPORT/);assert.doesNotMatch(route,/rawData/)});
